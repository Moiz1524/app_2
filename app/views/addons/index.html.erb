<h3>Addons</h3>
<% @addons.each do |a| %>
    <p><%= a.name %> | <%= a.price %></p>
    <p><%= link_to "Delete", a, data: {confirm: 'Are you sure you want to delete this Addon'}, :method => :delete %></p>
<% end %>
<%= link_to "Back to Items", items_path %>